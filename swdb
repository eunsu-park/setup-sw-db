#!/usr/bin/env bash
# Unified CLI for setup-sw-db.
# Dispatches to core/main.py with all arguments.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"
exec python -m core.main "$@"
